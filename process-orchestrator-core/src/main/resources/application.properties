# Database Configuration for Process Orchestrator
# PostgreSQL Database Settings

# Database connection details
db.url=jdbc:postgresql://localhost:5432/process_orchestrator
db.username=postgres
db.password=your-super-secret-and-long-postgres-password

# Connection pool settings
db.pool.initial.size=5
db.pool.max.size=20
db.pool.min.idle=5

# Connection timeout settings
db.connection.timeout=30000
db.socket.timeout=60000

# Test database settings (can override main settings for testing)
test.db.url=jdbc:postgresql://localhost:5432/process_orchestrator
test.db.username=postgres
test.db.password=your-super-secret-and-long-postgres-password
